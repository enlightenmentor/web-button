{
  "name": "web-button",
  "version": "0.0.1",
  "description": "Webcomponent web-button following open-wc recommendations",
  "author": "web-button",
  "homepage": "https://github.com/open-wc/open-wc/",
  "license": "MIT",
  "repository": "https://github.com/open-wc/open-wc/",
  "scripts": {
    "start": "owc-dev-server --open demo",
    "test": "karma start --coverage",
    "test:watch": "karma start --auto-watch=true --single-run=false",
    "test:update-snapshots": "karma start --update-snapshots",
    "test:prune-snapshots": "karma start --prune-snapshots",
    "test:legacy": "karma start --legacy --coverage",
    "test:legacy:watch": "karma start --legacy --auto-watch=true --single-run=false",
    "test:bs": "karma start karma.bs.config.js --legacy --coverage"
  },
  "dependencies": {
    "lit-element": "^2.0.1",
    "lit-html": "^1.0.0"
  },
  "devDependencies": {
    "@open-wc/testing": "^0.10.1",
    "@open-wc/testing-karma": "^1.0.0",
    "@open-wc/testing-karma-bs": "^1.0.0",
    "owc-dev-server": "^0.3.0",
    "webpack-merge": "^4.1.5"
  }
}
